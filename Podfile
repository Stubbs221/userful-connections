# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'UsefulConnections + MVP + Realm' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for UsefulConnections + MVP + Realm
	pod 'RealmSwift'
  target 'UsefulConnections + MVP + RealmTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'UsefulConnections + MVP + RealmUITests' do
    # Pods for testing
  end

end
